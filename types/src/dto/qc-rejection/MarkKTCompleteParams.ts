/**
 * Parameters for marking KT (Knowledge Transfer) as complete for an INVALID rejection
 */
export interface MarkKTCompleteParams {
  /** Optional notes about the KT acknowledgment */
  notes?: string;
}
